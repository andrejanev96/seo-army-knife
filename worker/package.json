{
  "name": "og-fetch-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies OG-tag fetches with rotating social-crawler User-Agents.",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
